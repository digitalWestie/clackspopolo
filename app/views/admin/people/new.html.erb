<h1>New Person</h1>
<%= form_for @person, url: admin_people_path, :html => { :class => "form-inline" } do |f| %>
  <%= render 'shared/errors', :object => @person %>
  <%= render 'form', :f => f %>
  <hr/>
  <%= f.submit "Save", :class => "btn" %> |
  <%= link_to 'Back to people', admin_people_path %>
<% end %>
