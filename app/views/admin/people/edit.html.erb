<h1>Edit Person</h1>
<%= form_for @person, url: admin_person_path(@person), :html => { :class => "form-inline" } do |f| %>
  <%= render 'shared/errors', :object => @person %>
  <%= render 'form', :f => f %>
  <hr/>
  <%= f.submit "Save", :class => "btn" %> |
  <%= link_to 'Back to people', admin_people_path %>
<% end %>
