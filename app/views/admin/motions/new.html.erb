<h1><%= link_to @organization, admin_organization_path(@organization) %>: New Motion</h1>

<%= form_for @motion, url: admin_organization_motions_path(@organization) do |f| %>
  
  <%= render 'form', f: f %>
  <%= f.submit 'Save' %>

<% end %>